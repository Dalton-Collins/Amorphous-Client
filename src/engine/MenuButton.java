package engine;

import javafx.scene.control.Button;
//button extender that allows buttons to store information about 
//what they represent so handlers can use it later.
public class MenuButton extends Button{
	int n;
	String s;
}
